public with sharing class Collections_check{
 
 public Collections_check(){
  }
Public static void updatefield(String so,String fi){
  
  
  if(so != null && fi == '') {
    
    system.debug('Provide a value');
    } 
     else{
     system.debug('success');
     }
    
  }
}